import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('xfile', () => {
    test('ok', async () => {
        const r1 = await request('http://xfile.test.whistlejs.com/index2.html');
        expect(r1.data).toHaveProperty('type', 'server');
    });
});

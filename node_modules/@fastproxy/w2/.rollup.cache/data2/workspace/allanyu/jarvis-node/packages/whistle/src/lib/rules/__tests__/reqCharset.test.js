import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('reqCharset', () => {
    test('ok', async () => {
        const r1 = await request('http://reqcharset.test.whistlejs.com/index.html');
        expect(r1.data.headers['content-type']).toContain('utf8');
    });
});

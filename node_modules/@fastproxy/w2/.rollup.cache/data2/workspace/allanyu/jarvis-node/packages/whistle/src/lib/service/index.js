import { fork } from 'pfork';
import { path } from '../import-pkg';
import config from '../config';
export default function loadService(callback) {
    fork({
        script: path.join(__dirname, 'service.js'),
        debugMode: config.debugMode
    }, callback);
}

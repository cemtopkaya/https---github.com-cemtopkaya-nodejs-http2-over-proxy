import { Transform } from 'pipestream';
import type { TransformOption } from './whistle-transform';
export default class SpeedTransform extends Transform {
    private _speed;
    private _delay;
    constructor(options: TransformOption);
    _transform(chunk: any, _encoding: string, callback: any): void;
}

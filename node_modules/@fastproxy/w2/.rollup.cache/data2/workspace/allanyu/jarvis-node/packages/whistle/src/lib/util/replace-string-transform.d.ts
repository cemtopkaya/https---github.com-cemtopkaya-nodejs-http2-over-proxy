import { Transform } from 'pipestream';
export default class ReplaceStringTransform extends Transform {
    private _str;
    private _length;
    private _value;
    private _rest;
    constructor(str: string, value: any);
    _transform: (chunk: any, _encoding: any, callback: any) => void;
}

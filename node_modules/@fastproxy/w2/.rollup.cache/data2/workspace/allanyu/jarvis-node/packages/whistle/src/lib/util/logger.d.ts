export interface Log {
    id: string | number;
    date: number;
    level: 'fatal' | 'error' | 'warn' | 'info' | 'debug' | 'log';
    text: string;
}
export type LogMethod = (msg: any, ...args: any[]) => void;
export interface Logger {
    getLogs: (startTime: number, count: number) => Log[];
    getLatestId: () => Log['id'];
    fatal: LogMethod;
    error: LogMethod;
    warn: LogMethod;
    info: LogMethod;
    log: LogMethod;
    debug: LogMethod;
}
declare const logger: Logger;
export default logger;

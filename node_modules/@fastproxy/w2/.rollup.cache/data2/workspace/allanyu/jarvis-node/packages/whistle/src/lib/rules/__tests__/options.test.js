import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('options', () => {
    test('ok', async () => {
        const r1 = await request('http://test.options.com/index.html');
        expect(r1.data.method).toBe('options');
    });
});

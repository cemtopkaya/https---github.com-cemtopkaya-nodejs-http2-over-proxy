import { describe, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('urlParams', () => {
    test('ok', async () => {
        const r1 = await request('http://urlparams.test.whistlejs.com/index.html');
        // data.url.substring(data.url.indexOf('?') +expect(1)).toBe('test=abc');
        const r2 = await request({
            method: 'post',
            url: 'http://urlparams.test.whistlejs.com/index.html'
        });
        // data.url.substring(data.url.indexOf('?') +expect(1)).toBe('test=abc');
    });
});

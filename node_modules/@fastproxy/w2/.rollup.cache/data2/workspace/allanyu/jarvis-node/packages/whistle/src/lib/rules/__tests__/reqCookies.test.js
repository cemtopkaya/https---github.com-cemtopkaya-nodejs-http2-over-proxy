import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('reqCookies', () => {
    test('ok', async () => {
        const r1 = await request('http://reqcookies.test.whistlejs.com/');
        expect(r1.data.headers.cookie).toBe('test=abc');
    });
});

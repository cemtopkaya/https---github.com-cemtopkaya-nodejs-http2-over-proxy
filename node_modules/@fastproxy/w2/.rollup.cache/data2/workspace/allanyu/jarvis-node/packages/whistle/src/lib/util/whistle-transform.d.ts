/// <reference types="node" />
import { Transform } from 'pipestream';
export interface TransformOption {
    speed: string;
    delay: string;
    charset?: string;
    strictHtml?: boolean;
    safeHtml?: boolean;
}
export default class WhistleTransform extends Transform {
    private _speed?;
    private _delay?;
    private _body;
    private _top;
    private _bottom;
    private _strictHtml?;
    private _safeHtml?;
    private _allowInject;
    private _ended;
    private _inited;
    constructor(options?: TransformOption);
    allowInject: (chunk: any) => boolean;
    _transform: (chunk: any, _encoding: string, callback: any) => void | NodeJS.Timeout;
}

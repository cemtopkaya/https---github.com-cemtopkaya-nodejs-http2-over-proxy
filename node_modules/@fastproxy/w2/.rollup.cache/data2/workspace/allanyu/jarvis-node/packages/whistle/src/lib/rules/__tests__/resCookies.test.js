import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('resCookies', () => {
    test('ok', async () => {
        const r1 = await request('http://rescookies.test.whistlejs.com/');
        expect(r1.res.headers['set-cookie']).toContain('key2=value2');
    });
});

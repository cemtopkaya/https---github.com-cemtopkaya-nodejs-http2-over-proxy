import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('reqType', () => {
    test('ok', async () => {
        const r1 = await request('http://restype.test.whistlejs.com/index.html');
        expect(r1.res.headers).toHaveProperty('content-type', 'text/html');
    });
});

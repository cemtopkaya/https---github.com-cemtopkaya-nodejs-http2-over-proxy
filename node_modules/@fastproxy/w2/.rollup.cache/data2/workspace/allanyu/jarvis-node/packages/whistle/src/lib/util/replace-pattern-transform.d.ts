import { Transform } from 'pipestream';
export default class ReplacePatternTransform extends Transform {
    private _pattern;
    private _replaceAll;
    private _value;
    private _rest;
    static replacePattern: (replacement: any, args: any) => any;
    constructor(pattern: any, value: any);
    _transform: (chunk: any, _: any, callback: any) => void;
}

import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('resType', () => {
    test('ok', async () => {
        const r1 = await request('http://reqtype.test.whistlejs.com/index.html');
        expect(r1.data.headers).toHaveProperty('content-type', 'text/plain');
    });
});

import { describe, expect, test } from 'vitest';
import { request } from '../../../__tests__/utils';
describe('resCharset', () => {
    test('ok', async () => {
        const r1 = await request('http://rescharset.test.whistlejs.com/index.html');
        expect(r1.res.headers['content-type']).toContain('utf8');
    });
});

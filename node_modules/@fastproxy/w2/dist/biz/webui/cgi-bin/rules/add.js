"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const util_1 = require("../../../../lib/rules/util");
function default_1(req, res) {
    const body = req.body;
    let list;
    util_1.rules.add(body.name, body.value, body.clientId);
    if (body.addToTop) {
        util_1.rules.moveToTop(body.name, body.clientId);
    }
    if (req.body.recycleFilename) {
        util_1.rules.getRecycleBin().remove(req.body.recycleFilename);
        list = util_1.rules.getRecycleBin().list();
    }
    res.json({
        ec: 0,
        list: list
    });
}
exports.default = default_1;
